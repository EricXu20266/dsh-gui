# ── Node / Electron ──
node_modules/
dist/
out/
*.tsbuildinfo

# ── DHS host 集成（运行时生成）──
tmp/
logs/
*.log

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db
Desktop.ini

# ── 环境 ──
.env
.env.*
!.env.example

# ── AI 运行时数据 ──
.taishen/
sessions/

# ── 打包产物 ──
release/
install/*.exe
install/*.msi

# ── dsh-discovery 插件构建产物与第三方 fork ──
plugins/dsh-discovery/lib/
plugins/dsh-discovery/client/
vendor/

# ── 打包运行时（node.exe / pnpm 从外部下载，不进仓库）──
resources/runtime/
